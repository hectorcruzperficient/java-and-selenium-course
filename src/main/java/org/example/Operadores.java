package org.example;

public class Operadores {
    // Tema 3: Operadores
    // Aritmeticos (+, -, *, /)
    // Unarios (++, --, +, -, !)
    // Relacionales (==, !=, >, <, =>, <=)
    // Condicionales (AND=&&, OR=||)
}
